fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
pandas==2.2.2
openpyxl==3.1.2
pydantic==2.7.4
python-multipart==0.0.9
pytest==8.2.2

# --- AI Agents + RAG (added for the agent layer) ---------------------------
anthropic>=0.40.0          # ROI Insight, Goal-Seeking, Decision Advisor agents
openai>=1.30.0             # OpenRouter client (OpenAI-compatible) for Knowledge Agent's RAG pipeline
pinecone==5.0.1            # Vector DB for Knowledge Agent
python-docx==1.1.0         # BRD ingestion (already used by file-reading skill, pinned here for the backend too)
tiktoken==0.6.0            # token-aware chunking; falls back to a word-count heuristic if its model file can't be downloaded
python-dotenv==1.0.0
httpx>=0.27.0
