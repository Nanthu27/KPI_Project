# ── Existing (DO NOT REMOVE) ──────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
pandas==2.2.2
openpyxl==3.1.2
pydantic==2.7.4
python-multipart==0.0.9
pytest==8.2.2

# ── AI Copilot (NEW — additive) ───────────────────────────────────────
# LLM: Google Gemini — called via plain stdlib HTTP (urllib), no SDK required.

# LangChain
langchain>=0.2.0
langchain-community>=0.2.0
langchain-core>=0.2.0

# LangGraph — multi-agent orchestration layer (app/ai/graph)
langgraph>=0.2.20

# Vector DB (optional — BM25 works without Pinecone)
pinecone>=6.0.0
langchain-pinecone>=0.1.0

# RAG / Search
rank-bm25>=0.2.2                # BM25 keyword search (no API key needed)

# Embeddings (optional — only needed for Pinecone ingestion)
sentence-transformers>=2.7.0

# Streaming
sse-starlette>=2.1.0

# Document loading
python-docx>=1.1.0              # For BRD .docx ingestion

# Env
python-dotenv>=1.0.0
pip-system-certs>=5.3
